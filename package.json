{
  "name": "conflict-resolution-engine",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic, replayable, auditable conflict-resolution engine for multi-agent document interpretations.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "test": "npm test --prefix server",
    "seed": "tsx scripts/seed.ts",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "tsx": "^4.19.2"
  }
}
